# Define a tuple with elements 'a', 'b', 'c', 'd', 'e'
tup = ('a', 'b', 'c', 'd', 'e')

# Loop through the tuple by its index using range and the length of the tuple
for i in range(len(tup)):
    # Print each element of the tuple by accessing it using its index
    print(tup[i])
