def concatenate_strings(input_tuple):
    # Join the elements of the tuple into a single string with spaces in between
    return ' '.join(input_tuple)

# Example usage: concatenate the tuple of strings ('Hello', 'world', 'from', 'Python')
print(concatenate_strings(('Hello', 'world', 'from', 'Python')))
